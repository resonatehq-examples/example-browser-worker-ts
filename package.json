{
  "name": "resonate-browser-demo",
  "version": "1.0.0",
  "description": "Browser demo for Resonate TypeScript SDK with factorial example",
  "main": "public/browser.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/worker.ts --bundle --format=esm --outfile=public/worker.js",
    "serve": "npx serve public",
    "dev": "bun run build && npx serve public"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "resonatehq",
    "typescript",
    "distributed",
    "async",
    "await"
  ],
  "author": "Resonate HQ, Inc",
  "license": "Apache-2.0",
  "dependencies": {
    "@resonatehq/sdk": "^0.11.0"
  }
}
